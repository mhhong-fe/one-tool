# 使用全局 pnpm store，避免在项目目录下创建 .pnpm-store
# 明确指定使用全局 store 路径
# macOS/Linux 默认路径: ~/.pnpm-store
# Windows 默认路径: %LOCALAPPDATA%/pnpm-store

# 使用绝对路径指向全局 store（推荐方式）
# 这会强制 pnpm 使用全局 store，而不是在项目目录下创建
store-dir=~/.pnpm-store
